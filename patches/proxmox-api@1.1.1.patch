diff --git a/cjs/ProxmoxEngine.js b/cjs/ProxmoxEngine.js
index 16be69d..88b9de1 100644
--- a/cjs/ProxmoxEngine.js
+++ b/cjs/ProxmoxEngine.js
@@ -122,7 +122,6 @@ class ProxmoxEngine {
             if (method === 'PUT' || method === 'POST') {
                 body = searchParams.toString();
                 headers['Content-Type'] = 'application/x-www-form-urlencoded';
-                headers['Content-Length'] = body.length.toString();
             }
         }
         let res = null;
@@ -246,7 +245,6 @@ class ProxmoxEngine {
             const headers = {
                 ...baseHeader,
                 'Content-Type': 'application/x-www-form-urlencoded',
-                'Content-Length': body.length.toString(),
             };
             const controller = new AbortController();
             const id = setTimeout(() => controller.abort(), this.authTimeout);
diff --git a/esm/ProxmoxEngine.js b/esm/ProxmoxEngine.js
index 7722b42..60b3b1e 100644
--- a/esm/ProxmoxEngine.js
+++ b/esm/ProxmoxEngine.js
@@ -119,7 +119,6 @@ export class ProxmoxEngine {
             if (method === 'PUT' || method === 'POST') {
                 body = searchParams.toString();
                 headers['Content-Type'] = 'application/x-www-form-urlencoded';
-                headers['Content-Length'] = body.length.toString();
             }
         }
         let res = null;
@@ -243,7 +242,6 @@ export class ProxmoxEngine {
             const headers = {
                 ...baseHeader,
                 'Content-Type': 'application/x-www-form-urlencoded',
-                'Content-Length': body.length.toString(),
             };
             const controller = new AbortController();
             const id = setTimeout(() => controller.abort(), this.authTimeout);
diff --git a/src/ProxmoxEngine.ts b/src/ProxmoxEngine.ts
index 94a1961..f8c4826 100644
--- a/src/ProxmoxEngine.ts
+++ b/src/ProxmoxEngine.ts
@@ -198,7 +198,6 @@ export class ProxmoxEngine implements ApiRequestable {
             if (method === 'PUT' || method === 'POST') {
                 body = searchParams.toString()
                 headers['Content-Type'] = 'application/x-www-form-urlencoded';
-                headers['Content-Length'] = body.length.toString();
             }
         }
         let res: Response | null = null;
@@ -317,7 +316,6 @@ export class ProxmoxEngine implements ApiRequestable {
             const headers = {
                 ...baseHeader,
                 'Content-Type': 'application/x-www-form-urlencoded',
-                'Content-Length': body.length.toString(),
             }
             const controller = new AbortController();
             const id = setTimeout(() => controller.abort(), this.authTimeout);
