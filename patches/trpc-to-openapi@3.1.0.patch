diff --git a/dist/cjs/generator/schema.js b/dist/cjs/generator/schema.js
index 402a311b64e61ccb02b9a474fe41d7c80be6b6e2..98772cf37448897bdeee33587a82ee60e6b324a1 100644
--- a/dist/cjs/generator/schema.js
+++ b/dist/cjs/generator/schema.js
@@ -91,7 +91,20 @@ const getRequestBodyObject = (schema, required, pathParameters, contentTypes) =>
         mask[pathParameter] = true;
     });
     const o = schema.meta();
-    const dedupedSchema = schema.omit(mask).meta(Object.assign(Object.assign(Object.assign({}, ((o === null || o === void 0 ? void 0 : o.title) ? { title: o === null || o === void 0 ? void 0 : o.title } : {})), ((o === null || o === void 0 ? void 0 : o.description) ? { description: o === null || o === void 0 ? void 0 : o.description } : {})), ((o === null || o === void 0 ? void 0 : o.examples) ? { examples: o === null || o === void 0 ? void 0 : o.examples } : {})));
+    let dedupedSchema;
+    try {
+        dedupedSchema = schema.omit(mask);
+    }
+    catch {
+        const filteredShape = {};
+        for (const [key, value] of Object.entries(schema.shape)) {
+            if (!mask[key]) {
+                filteredShape[key] = value;
+            }
+        }
+        dedupedSchema = zod_1.z.object(filteredShape);
+    }
+    dedupedSchema = dedupedSchema.meta(Object.assign(Object.assign(Object.assign({}, ((o === null || o === void 0 ? void 0 : o.title) ? { title: o === null || o === void 0 ? void 0 : o.title } : {})), ((o === null || o === void 0 ? void 0 : o.description) ? { description: o === null || o === void 0 ? void 0 : o.description } : {})), ((o === null || o === void 0 ? void 0 : o.examples) ? { examples: o === null || o === void 0 ? void 0 : o.examples } : {})));
     // if all keys are path parameters
     if (pathParameters.length > 0 && Object.keys(dedupedSchema.shape).length === 0) {
         return undefined;
diff --git a/dist/esm/generator/schema.mjs b/dist/esm/generator/schema.mjs
index 0000000000000000000000000000000000000000..1111111111111111111111111111111111111111 100644
--- a/dist/esm/generator/schema.mjs
+++ b/dist/esm/generator/schema.mjs
@@ -91,7 +91,20 @@ export const getRequestBodyObject = (schema, required, pathParameters, contentTy
         mask[pathParameter] = true;
     });
     const o = schema.meta();
-    const dedupedSchema = schema.omit(mask).meta({
+    let dedupedSchema;
+    try {
+        dedupedSchema = schema.omit(mask);
+    }
+    catch {
+        const filteredShape = {};
+        for (const [key, value] of Object.entries(schema.shape)) {
+            if (!mask[key]) {
+                filteredShape[key] = value;
+            }
+        }
+        dedupedSchema = z.object(filteredShape);
+    }
+    dedupedSchema = dedupedSchema.meta({
         ...(o?.title ? { title: o?.title } : {}),
         ...(o?.description ? { description: o?.description } : {}),
         ...(o?.examples ? { examples: o?.examples } : {}),
diff --git a/dist/cjs/utils/zod.js b/dist/cjs/utils/zod.js
index cd7ed5975d77e9c4ad110a7e1062365e2c242a91..e6ebd712a094f19dc80c8a07b96a4dffa4046a51 100644
--- a/dist/cjs/utils/zod.js
+++ b/dist/cjs/utils/zod.js
@@ -43,10 +43,19 @@ const unwrapZodType = (type, unwrapPreprocess) => {
     if ((0, exports.instanceofZodTypeKind)(type, 'default')) {
         return (0, exports.unwrapZodType)(type.unwrap(), unwrapPreprocess);
     }
+    if ((0, exports.instanceofZodTypeKind)(type, 'prefault')) {
+        return (0, exports.unwrapZodType)(type.unwrap(), unwrapPreprocess);
+    }
     if ((0, exports.instanceofZodTypeKind)(type, 'lazy')) {
         return (0, exports.unwrapZodType)(type.def.getter(), unwrapPreprocess);
     }
     if ((0, exports.instanceofZodTypeKind)(type, 'pipe') && unwrapPreprocess) {
+        const pipeDef = type._zod?.def ?? type.def;
+        const outType = pipeDef?.out ?? type.def?.out;
+        if (outType && (0, exports.instanceofZodTypeKind)(outType, 'transform')) {
+            const inType = pipeDef?.in ?? type.def?.in;
+            return (0, exports.unwrapZodType)(inType, unwrapPreprocess);
+        }
         return (0, exports.unwrapZodType)(type.def.out, unwrapPreprocess);
     }
     return type;
diff --git a/dist/esm/utils/zod.mjs b/dist/esm/utils/zod.mjs
index a3fda1a5b5403a659bfc70653a94102e607f8b80..5798f801a2fadcbbb8b71cbc8d3a3046aa204c64 100644
--- a/dist/esm/utils/zod.mjs
+++ b/dist/esm/utils/zod.mjs
@@ -33,10 +33,19 @@ export const unwrapZodType = (type, unwrapPreprocess) => {
     if (instanceofZodTypeKind(type, 'default')) {
         return unwrapZodType(type.unwrap(), unwrapPreprocess);
     }
+    if (instanceofZodTypeKind(type, 'prefault')) {
+        return unwrapZodType(type.unwrap(), unwrapPreprocess);
+    }
     if (instanceofZodTypeKind(type, 'lazy')) {
         return unwrapZodType(type.def.getter(), unwrapPreprocess);
     }
     if (instanceofZodTypeKind(type, 'pipe') && unwrapPreprocess) {
+        const pipeDef = type._zod?.def ?? type.def;
+        const outType = pipeDef?.out ?? type.def?.out;
+        if (outType && instanceofZodTypeKind(outType, 'transform')) {
+            const inType = pipeDef?.in ?? type.def?.in;
+            return unwrapZodType(inType, unwrapPreprocess);
+        }
         return unwrapZodType(type.def.out, unwrapPreprocess);
     }
     return type;
