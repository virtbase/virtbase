{
  "name": "@virtbase/auth",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./utils": "./src/utils.ts",
    "./env": "./env.ts",
    "./stored-oauth-token": "./src/stored-oauth-token.ts"
  },
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "test": "bun --env-file=../../.env.test test --timeout 20000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@better-auth/api-key": "catalog:",
    "@better-auth/passkey": "catalog:",
    "@sentry/core": "catalog:sentry",
    "@t3-oss/env-core": "^0.13.11",
    "@virtbase/db": "workspace:*",
    "@virtbase/email": "workspace:*",
    "@virtbase/utils": "workspace:*",
    "@virtbase/validators": "workspace:*",
    "better-auth": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@virtbase/tsconfig": "workspace:*",
    "typescript": "catalog:"
  }
}
