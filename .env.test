# Every value here is an inert placeholder. Tests inject their own PGlite
# instance, so this connection string is constructed and never connected to.
# Never put a real secret in this file.
DATABASE_URL=postgresql://test:test@localhost:5432/virtbase_test
NEXT_PUBLIC_APP_NAME=Virtbase
NEXT_PUBLIC_APP_DOMAIN=virtbase.com
BETTER_AUTH_SECRET=test-only-secret-not-used-to-sign-anything-real
